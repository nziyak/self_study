/* += first addition then assignment   a+=1 is equivalent to a = a + 1
   -= first substraction then assignment
   *= first multiplication then assignment
   /= first division then assignment
   %= first modulus then assignment
   <<= first bitwise left shift then " 
   &= first bitwise AND then "
   |= first bitwise OR then "
   ^= first bitwise XOR then " */

   #include <stdio.h>

   int main(){
   
   char a = 7;
   a ^= 5;
   printf("%d", printf("%d", a+=3));
   return 0;

   }